---
export interface Props {
	genre: String
}

const { genre } = Astro.props;
---
<a href={`/genre/${genre}`} class="transform rounded bg-black p-1 text-xs text-white transition duration-300 ease-out hover:scale-105 sm:text-sm">
    {genre}
</a>